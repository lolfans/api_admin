stages:
- name: step1
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: api-admin
      pushRemote: true
      registry: registry.cn-qingdao.aliyuncs.com
    when:
      branch:
        include:
        - master
timeout: 60
notification: {}
